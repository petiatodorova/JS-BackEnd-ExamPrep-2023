<li class="card">
    <img src="{{animal.image}}" />
    <p>
        <strong>Name: </strong><span class="name">{{animal.name}}</span>
    </p>
    <p>
        <strong>Location: </strong><span class="location">{{animal.location}}</span>
    </p>
    <p><strong>Need of:</strong><span class="need-of">{{animal.need}}</span></p>
    <a class="details-btn" href="/animals/{{_id}}/details">Details</a>
</li>